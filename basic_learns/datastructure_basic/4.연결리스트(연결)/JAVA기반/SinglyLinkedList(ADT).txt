/*리스트의 요소 수를 반환*/
size();

/*리스트가 비어 있으면 true, 아니면 false*/
isEmpty();

/*리스트의 첫번째 요소 반환*/
first();

/*리스트의 마지막 요소 반환*/
last();

/*리스트의 맨 앞에 추가*/
addFirst(E e);

/*리스트의 맨 뒤에 추가*/
addLast(E e);

/*리스트의 맨앞 요소를 제거 & 반환 */
removeFirst();
